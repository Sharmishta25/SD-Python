#food bill
import random
names=input("Enter everyones names separated by comma:")
name_list=names.split(" , ")
length=len(name_list)
random_choice= random.randint(0,length-1)
print(f"{name_list[random_choice]} will pay the bill")